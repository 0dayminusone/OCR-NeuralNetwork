enum SortOrder { asc, desc }

enum TextSearchType { context, filename, description, ocrText }

enum AssetVisibilityEnum { timeline, hidden, archive, locked }

enum SortUserBy { id }

enum ActionSource { timeline, viewer }
